import { describe, it, expect } from 'vitest'

describe('GET /api/ligas/[slug]/previsao', () => {
  it('deve retornar 400 sem homeTeamId e awayTeamId', async () => {
    expect(true).toBe(true)
  })
})
