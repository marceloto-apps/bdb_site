import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'

describe('SeletorConfronto', () => {
  it('deve renderizar sem crash', () => {
    expect(true).toBe(true)
  })
})
