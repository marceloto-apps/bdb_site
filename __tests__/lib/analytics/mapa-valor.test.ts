import { describe, it, expect } from 'vitest'

describe('Mapa de Valor — Cálculo de ROI', () => {
  it('deve categorizar jogos nas faixas corretas de odds', () => {
    expect(true).toBe(true)
  })
})
