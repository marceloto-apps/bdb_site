import { describe, it, expect } from 'vitest'

describe('Seletor de Modelo via AIC', () => {
  it('deve retornar POISSON como default quando dados são homogêneos', () => {
    expect(true).toBe(true)
  })
})
